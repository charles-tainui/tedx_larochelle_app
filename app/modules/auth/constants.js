import {
	FACEBOOK_APP_ID as fbID,
	GOOGLE_APP_ID as googleID,
} from "../../config/constants"

export const NAME = 'auth';

export const FACEBOOK_APP_ID = fbID;
export const GOOGLE_APP_ID = googleID;